import { NormalizeIdPipe } from './normalize-id.pipe';

describe('NormalizeIdPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeIdPipe();
    expect(pipe).toBeTruthy();
  });
});
