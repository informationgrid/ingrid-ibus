# the port used during development
server.port = 8083

# special flag to deactivate some security stuff
development = true

# do not restart server when the following files are changed
spring.devtools.restart.exclude=activatedIplugs.properties

codelistrepo.username=admin
codelistrepo.password=admin

# during development the frontend is being build in the target directory
# spring.resources.static-locations=file:./target/frontend/